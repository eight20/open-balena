apiVersion: v1
kind: Service
metadata:
  name: {{ include "openbalena.redis.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "openbalena.redis.labels" . | nindent 4 }}
  {{- with .Values.redis.service.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  selector:
    {{- include "openbalena.redis.selectorLabels" . | nindent 4 }}
  ports:
    {{- range .Values.redis.service.ports }}
    - port: {{ .port }}
      {{- if .targetPort }}
      targetPort: {{ .targetPort }}
      {{- end }}
      {{- if .protocol }}
      protocol: {{ .protocol }}
      {{- end }}
      {{- if .name }}
      name: {{ .name }}
      {{- end }}
    {{- end }}
  {{- with .Values.redis.service.externalIPs }}
  externalIPs:
    {{- toYaml . | indent 4 }}
  {{- end }}
