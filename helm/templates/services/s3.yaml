apiVersion: v1
kind: Service
metadata:
  name: {{ include "openbalena.s3.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "openbalena.s3.labels" . | nindent 4 }}
  {{- with .Values.s3.service.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  selector:
    {{- include "openbalena.s3.selectorLabels" . | nindent 4 }}
  ports:
    {{- range .Values.s3.service.ports }}
    - port: {{ .port }}
      {{- if .targetPort }}
      targetPort: {{ .targetPort }}
      {{- end }}
      {{- if .protocol }}
      protocol: {{ .protocol }}
      {{- end }}
      {{- if .name }}
      name: {{ .name }}
      {{- end }}
    {{- end }}
  {{- with .Values.s3.service.externalIPs }}
  externalIPs:
    {{- toYaml . | indent 4 }}
  {{- end }}
