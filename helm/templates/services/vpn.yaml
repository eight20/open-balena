apiVersion: v1
kind: Service
metadata:
  name: {{ include "openbalena.vpn.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "openbalena.vpn.labels" . | nindent 4 }}
  {{- with .Values.vpn.service.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  selector:
    {{- include "openbalena.vpn.selectorLabels" . | nindent 4 }}
  ports:
    {{- range .Values.vpn.service.ports }}
    - port: {{ .port }}
      {{- if .targetPort }}
      targetPort: {{ .targetPort }}
      {{- end }}
      {{- if .protocol }}
      protocol: {{ .protocol }}
      {{- end }}
      {{- if .name }}
      name: {{ .name }}
      {{- end }}
    {{- end }}
  {{- with .Values.vpn.service.externalIPs }}
  externalIPs:
    {{- toYaml . | indent 4 }}
  {{- end }}
